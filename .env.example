# Copy to .env and fill in. Never commit .env.

# PostHog project API key (public client key, starts with "phc_").
# Required: posthog-react-native throws at startup without it, which also
# breaks `bun build:web`. Set it in the Cloudflare Workers Builds env too.
EXPO_PUBLIC_POSTHOG_API_KEY=
